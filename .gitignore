# pixi
.pixi/

# python
__pycache__/
*.pyc
.ruff_cache/
.ipynb_checkpoints/

# generated artifacts (rebuilt from live sources each run):
# the map's derived layers (derive stage) and the dataset downloads (ingest stage)
site/map/data/
site/data/

# incremental per-day field store (build-time cache; never served, see _field_store)
cache/

# intermediate downloads
*.nc
*.zip

# experiments / scratch (per AGENTS.md)
tmp_*/

# macOS
.DS_Store
