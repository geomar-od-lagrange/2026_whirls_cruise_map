[pytest]
# Hard per-test cap (pytest-timeout) so a pathological test or hung I/O fails
# loudly instead of wedging an agent, cron, or CI run. Generous: the slowest
# legitimate test (multi-day StoreField advection) runs in single-digit seconds.
timeout = 120
